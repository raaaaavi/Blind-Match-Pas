@{
    Layout = "_AuthLayout";
    ViewData["Title"] = "Access Denied";
}

<div class="text-center">
    <div class="display-5 mb-3"><i class="bi bi-shield-lock"></i></div>
    <h2>Access denied</h2>
    <p class="text-secondary">Your current role does not have permission to view this area.</p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary mt-3">Return Home</a>
</div>
